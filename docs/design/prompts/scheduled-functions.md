Implement scheduled functions per plans/functions-scheduler.md (committed on this branch). Read that plan in full before designing — it specifies the boa.json schema additions (schedule, scheduleTimezone, scheduleInput), the visibility:private invariant, the EventBridge Scheduler -> FunctionsLambda direct-invoke trust path, the runtime ctx.role='service_role' rule for scheduled invocations, the nested-stack CloudFormation pattern (FunctionsScheduleRole + FunctionsSchedulesStack), the deploy/list/verify CLI extensions, the unit tests in cli/__tests__/, the live E2E in plans/e2e-testing-plan.md, the manual entries in plans/manual-test-plan.md, and the new skill eval. The launch gate is: every cli/__tests__/ file passes in CI, the live E2E (steps 1-8) runs green on a clean account, the new eval passes in a fresh Claude Code session, and all five manual entries pass. Implementation order: discovery+validation+tests -> nested-stack generation+tests -> runtime auth path+tests -> CloudFormation deltas -> deploy wiring -> list/verify CLI -> E2E+manual+eval -> skill+docs (docs last per the saved 'never document what you haven't deployed' rule).