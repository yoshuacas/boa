Implement BOA Functions per plans/functions.md (committed on this branch). Read that plan in full before designing — it specifies the file shape, token model, visibility, packaging (single shared FunctionsLambda), CLI subcommands, CloudFormation deltas, skill/doc updates, and the complete testing strategy (unit tests in cli/__tests__/, integration E2E in plans/e2e-testing-plan.md, manual entries in plans/manual-test-plan.md, and three skill evals in plugin/skills/boa/evals/evals.json). The launch gate is: every cli/__tests__/ file passes, the live E2E runs green, all three evals pass with the production skill in a fresh Claude Code session, and manual test entries are checked off. Implementation order: runtime+tests → discovery+packaging+tests → CloudFormation → scaffold+example → CLI subcommands → verify → E2E+manual+evals → skill+docs (docs last).